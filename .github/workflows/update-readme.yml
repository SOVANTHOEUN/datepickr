name: Update README
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity
    steps:
      - uses: ./.github/actions/checkout@master
      - uses: SOVANTHOEUN/datepickr@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          fetch-depth: 0
          repository: SOVANTHOEUN/datepickr
          COMMIT_MSG: "Updated README with recent activity"
          MAX_LINES: 10
